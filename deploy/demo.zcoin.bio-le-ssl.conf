<VirtualHost *:443>
    ServerName demo.zcoin.bio

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/demo.zcoin.bio/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/demo.zcoin.bio/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf

    ProxyPreserveHost On

    # WebSocket support
    RewriteEngine On
    RewriteCond %{HTTP:Upgrade} websocket [NC]
    RewriteCond %{HTTP:Connection} upgrade [NC]
    RewriteRule ^/(.*) ws://192.168.88.7:3100/$1 [P,L]

    # Reverse proxy to dev server (same backend as zcoin.bio)
    ProxyPass / http://192.168.88.7:3100/
    ProxyPassReverse / http://192.168.88.7:3100/

    ErrorLog ${APACHE_LOG_DIR}/demo.zcoin.bio-ssl-error.log
    CustomLog ${APACHE_LOG_DIR}/demo.zcoin.bio-ssl-access.log combined
</VirtualHost>
