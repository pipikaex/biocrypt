<VirtualHost *:80>
    ServerName zcoin.bio
    ServerAlias www.zcoin.bio

    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</VirtualHost>
